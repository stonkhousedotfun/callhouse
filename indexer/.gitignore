# Ponder's local database and build output. `ponder-env.d.ts` is NOT here: it is generated by
# `ponder codegen` and must be committed, because tsc needs it to resolve the `ponder:*`
# virtual modules.
.ponder/
generated/
.env.local
